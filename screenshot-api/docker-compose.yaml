

services:
  screenshot-api:
    build: .
    image: screenshot-api
    container_name: screenshot-api
    ports:
      - "127.0.0.1:4001:4001"
    shm_size: "512mb"
    deploy:
      resources:
        limits:
          memory: 700M
          cpus: "1.0"
    restart: unless-stopped

